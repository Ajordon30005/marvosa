# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    # No configuration required — the organism is fully self-contained.
    type: object
    properties: {}
  commandFunction:
    # A function that produces the CLI command to start the MCP on stdio.
    |-
    (config) => ({ command: 'python3', args: ['hcl-ai/marvosa_mcp.py'] })
  exampleConfig: {}
